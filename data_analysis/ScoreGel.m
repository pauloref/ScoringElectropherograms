function [outputArg1,outputArg2] = ScoreGel(input_data,gel_list)
%ScoreGel This function ranks the gels according to how separated their
%peaks are and how wide they are
%   INPUTS: gel_list corresponds to a cell containing the name of the gels
%           as strings
% user must select the right csv table to import the peak data so that it
% can be analysed. 
peak_file = uigetfile(); %retrieve peak file


end

