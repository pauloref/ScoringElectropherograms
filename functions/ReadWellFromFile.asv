function [output] = ReadWellFromFile(filename)
    content = importdata(file.name);
    data = content.data(:,[1,3,5,7,9]);
    file = dir(filename);
    content = importdata(file.name);
    outpu
    contents = importData(file);
    contents = file.data(:,[1,3,5,7,9,11]);
   
    
end